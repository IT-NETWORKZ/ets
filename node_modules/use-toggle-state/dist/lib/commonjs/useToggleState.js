"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useToggleState = void 0;
var tslib_1 = require("tslib");
var React = tslib_1.__importStar(require("react"));
var util_1 = require("util");
/**
 * A useState like hook for boolean values that returns the state, and a ToggleStateAction instead of a SetStateAction that allows
 * the setState callback to be called without passing any arguments to simply toggle the boolean value.
 *
 * @param initialState
 */
function useToggleState(initialState) {
    var _a = React.useState(initialState !== null && initialState !== void 0 ? initialState : false), state = _a[0], setState = _a[1];
    var toggle = React.useCallback(function (value) {
        setState(function (prevState) {
            // If we are being told which value to use, use it
            if (!util_1.isNullOrUndefined(value)) {
                if (value instanceof Function) {
                    return value(prevState);
                }
                return value;
            }
            // Otherwise simply toggle the value.
            return !prevState;
        });
    }, [setState]);
    return [state, toggle];
}
exports.useToggleState = useToggleState;
//# sourceMappingURL=useToggleState.js.map